export const QUERY_KEYS = {
  CARS: ['fetch-cars-list'],
  MUTATE_CAR: ['cars', 'favorite', 'mutate'],
  MUTATE_CAR_BID: ['cars', 'bid', 'mutate-bid'],
};

export const TIME_IN_MS = {
  ONE_HOUR: 60 * 60 * 1000,
};
